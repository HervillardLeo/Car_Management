<table>
    <tr>
        <th>Nom</th>
        <th>Marque</th>
        <th>Année</th>
        <th>Puissance (CV)</th>
    </tr>
    {% for car in cars %}
        <tr class="{{car.colorClass}}">
            <td class="click-car" data-car-id="{{ car.id }}"><a href="#">{{ car.modelName }}</a></td>
            <td>{{ car.brand }}</td>
            <td>{{ car.year | date("Y") }}</td>
            <td>{{ car.power }} ch</td>
        </tr>
    {% endfor %}
</table>